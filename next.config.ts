import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  "images": { "domains": ["images.unsplash.com","motorepuestos.vtexassets.com", "lh3.googleusercontent.com" ]},
};

export default nextConfig;
